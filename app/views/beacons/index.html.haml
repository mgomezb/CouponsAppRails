%h1 Listing beacons

%table.table
  %tr
    %th Major
    %th Minor
    %th Proximity uuid
    %th Description
    %th Local
    %th
    %th
    %th

  - @beacons.each do |beacon|
    %tr
      %td= beacon.major
      %td= beacon.minor
      %td= beacon.proximity_uuid
      %td= beacon.description
      %td= beacon.local
      %td= link_to 'Show', local_beacon_path(@local, beacon)
      %td= link_to 'Edit', edit_local_beacon_path(@local, beacon)
      %td= link_to 'Destroy', local_beacon_path(@local, beacon), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Beacon', new_beacon_path
