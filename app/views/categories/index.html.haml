%h1 Categorías

= link_to 'Nueva Category', new_category_path, remote: true,  class: 'btn btn-success'
%p

%table.table
  %tr
    %th Categoría
    %th
    %th

  - @categories.each do |category|
    %tr
      %td= category.name
      %td
        = link_to edit_category_path(category) do
          %i.fa.fa-edit{style: 'color: orange'}
      %td
        = link_to category, :method => :delete, :data => { :confirm => '¿Estás seguro?' } do
          %i.fa.fa-times{style: 'color: red'}

%br

#newCategoryModal.modal.fade{"aria-hidden" => "true", "aria-labelledby" => "newCategoryModalLabel", :role => "dialog", :tabindex => "-1"}
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{"data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} ×
          %span.sr-only Close
        %h4#myModalLabel.modal-title Crear categoría
      .modal-body
        = render 'categories/form'


